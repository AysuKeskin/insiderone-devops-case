apiVersion: v1
kind: Service
metadata:
  name: {{ include "insiderone-devops-case.fullname" . }}
  labels:
    {{- include "insiderone-devops-case.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "insiderone-devops-case.selectorLabels" . | nindent 4 }}
