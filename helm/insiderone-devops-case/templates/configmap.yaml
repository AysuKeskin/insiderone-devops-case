apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "insiderone-devops-case.fullname" . }}-config
  labels:
    {{- include "insiderone-devops-case.labels" . | nindent 4 }}
data:
  {{- range $key, $val := .Values.config }}
  {{ $key }}: {{ $val | quote }}
  {{- end }}
