apiVersion: v1
kind: Service
metadata:
  name: {{ include "kube-pulse.fullname" . }}
  labels:
    {{- include "kube-pulse.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "kube-pulse.selectorLabels" . | nindent 4 }}
