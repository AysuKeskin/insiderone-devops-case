# Copy to .env for local development. .env is gitignored.

# App listen port inside the container. Must be >= 1024 (non-root user).
PORT=8080

# Host port mapped by docker compose. Change if 8080 is in use locally.
HOST_PORT=8080

# slog level: debug | info | warn | error
LOG_LEVEL=debug
